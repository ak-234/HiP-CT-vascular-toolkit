[build-system]
requires = ["scikit-build-core>=0.10", "pybind11>=2.12"]
build-backend = "scikit_build_core.build"

[project]
name = "coronary-sdf-cgal"
version = "0.3.0"
description = "Native CGAL Mesh_3 backend for coronary_sdf"
requires-python = ">=3.12"

[tool.scikit-build]
cmake.build-type = "Release"
wheel.packages = []
